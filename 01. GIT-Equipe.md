# 🚀 Exercício — Desenvolvimento Java em Equipe com Git e GitHub

Neste exercício, a equipe desenvolverá um pequeno sistema Java utilizando **Orientação a Objetos, Herança e Polimorfismo**.

O tema poderá ser escolhido pela equipe entre as opções apresentadas neste enunciado.

Independentemente do tema escolhido, **todas as equipes deverão seguir exatamente as mesmas regras técnicas e o mesmo fluxo de trabalho com Git e GitHub**.

> 👥 O trabalho deverá ser realizado em **trios**.

---

# 🎯 Objetivos 

O objetivo deste exercício é simular uma situação real de desenvolvimento em equipe.

Os três integrantes trabalharão no mesmo repositório, porém cada integrante desenvolverá sua funcionalidade em uma branch diferente.

Ao final da atividade, a equipe deverá ser capaz de:

* modelar uma hierarquia de classes utilizando herança;
* utilizar atributos e métodos em uma superclasse;
* criar subclasses especializadas;
* sobrescrever métodos utilizando `@Override`;
* aplicar polimorfismo;
* criar e trabalhar com branches;
* realizar commits;
* enviar branches para o GitHub;
* abrir Pull Requests;
* realizar Code Review;
* integrar alterações desenvolvidas por diferentes integrantes;
* atualizar uma branch utilizando `rebase`;
* identificar e resolver conflitos;
* compreender o fluxo de desenvolvimento colaborativo.
  
---

# 📊 Fluxo completo

```text
                    GITHUB

                     main
                      ↑
                      │
               Pull Request
                      │
                     dev
                ↗     ↑     ↖
               /      │      \
              /       │       \
         PR + Review  │   PR + Review
            /         │         \
           /          │          \
feature/          feature/       feature/
superclasse       subclasse-a    subclasse-b
     ↑                 ↑              ↑
     │                 │              │
 Codespace A       Codespace B     Codespace C
 Pessoa A          Pessoa B        Pessoa C
```

---

# ⚠️ Antes de começar

Neste exercício, todo o desenvolvimento será realizado no GitHub Codespaces.
Não será necessário:
* clonar o projeto para o computador;
* utilizar Git Bash;
* utilizar CMD;
* configurar Java localmente;
* configurar Git localmente.
  
Todos os comandos deverão ser executados no terminal do Codespaces.

---

# 👥 Organização da equipe

A equipe será formada por **3 integrantes**.

Cada integrante deverá ser responsável por uma parte do desenvolvimento.

| Integrante | Responsabilidade principal                    |
| ---------- | --------------------------------------------- |
| Pessoa A   | Superclasse + estrutura inicial do projeto    |
| Pessoa B   | Primeira subclasse                            |
| Pessoa C   | Segunda subclasse                             |
| Equipe     | Integração e Main                             |

⚠️ **Cada integrante deverá realizar seus próprios commits utilizando sua conta do GitHub.**

Não será considerado trabalho em equipe um projeto no qual apenas uma pessoa realizou todos os commits.

---

# 🎨 Escolha do tema

A equipe deverá escolher **UM** dos temas abaixo.

O tema altera apenas o contexto do sistema.

As regras de implementação são as mesmas para todas as equipes.

## Opção 1 — Animais

```text
Animal
 ├── Cachorro
 └── Gato
```

Exemplos de comportamentos:

* emitirSom();
* mover();
* exibirInformacoes().

---

## Opção 2 — Personagens de Jogo

```text
Personagem
 ├── Guerreiro
 └── Mago
```

Exemplos de comportamentos:

* atacar();
* usarHabilidade();
* exibirInformacoes().

---

## Opção 3 — Funcionários

```text
Funcionario
 ├── Desenvolvedor
 └── Gerente
```

Exemplos de comportamentos:

* trabalhar();
* calcularBonus();
* exibirInformacoes().

---

## Opção 4 — Veículos

```text
Veiculo
 ├── Carro
 └── Moto
```

Exemplos de comportamentos:

* acelerar();
* mover();
* exibirInformacoes().

---

## Opção 5 — Produtos

```text
Produto
 ├── Livro
 └── Eletronico
```

Exemplos de comportamentos:

* calcularPrecoFinal();
* exibirDetalhes();
* exibirInformacoes().

---

## Opção 6 — Contas Bancárias

```text
Conta
 ├── ContaCorrente
 └── ContaPoupanca
```

Exemplos de comportamentos:

* calcularRendimento();
* exibirSaldo();
* exibirInformacoes().

---

## Opção 7 — Instrumentos Musicais

```text
Instrumento
 ├── Violao
 └── Piano
```

Exemplos de comportamentos:

* tocar();
* afinar();
* exibirInformacoes().

---

## Opção 8 — Dispositivos Eletrônicos

```text
Dispositivo
 ├── Smartphone
 └── Notebook
```

Exemplos de comportamentos:

* ligar();
* executarAcao();
* exibirInformacoes().

---

## Opção 9 — Transporte

```text
Transporte
 ├── Onibus
 └── Aviao
```

Exemplos de comportamentos:

* mover();
* calcularTempoViagem();
* exibirInformacoes().

---

## Opção 10 — Tema próprio

A equipe também poderá propor outro tema.

Entretanto, obrigatoriamente deverá existir:

```text
Superclasse
 ├── SubclasseA
 └── SubclasseB
```

O tema próprio **não altera nenhum dos requisitos técnicos da atividade**.

---

# ✅ Regras obrigatórias para TODOS os temas

Toda equipe deverá implementar:
 ```text
       Superclasse
            ↑
       ┌────┴────┐
       │         │
 Subclasse A  Subclasse B
 ```
Também deverá existir:
```text
Main.java
```
O Main.java será utilizado para testar o sistema completo.

---

# 🧱 1. Superclasse

A superclasse deverá possuir obrigatoriamente:

* pelo menos **2 atributos privados**;
* construtor;
* getters e setters;
* método `toString()`;
* pelo menos **1 método que será sobrescrito pelas subclasses**.

Exemplo conceitual:

```java
public class Superclasse {

    private String nome;
    private int codigo;

    public Superclasse(String nome, int codigo) {
        this.nome = nome;
        this.codigo = codigo;
    }

    public void executarAcao() {
        System.out.println("Executando ação genérica");
    }

}
```

⚠️ Este código é apenas um exemplo de estrutura.

A equipe deverá adaptá-lo ao tema escolhido.

---

# 🧩 2. Primeira subclasse

A primeira subclasse deverá:

* utilizar `extends`;
* possuir pelo menos **1 atributo próprio**;
* possuir construtor;
* possuir getter e setter do atributo próprio;
* sobrescrever pelo menos **1 método da superclasse**;
* utilizar a anotação `@Override`.

Estrutura esperada:

```java
public class SubclasseA extends Superclasse {

    private String atributoEspecifico;

    @Override
    public void executarAcao() {
        // comportamento específico
    }

}
```

---

# 🧩 3. Segunda subclasse

A segunda subclasse deverá seguir as mesmas regras:

* utilizar `extends`;
* possuir pelo menos **1 atributo próprio**;
* possuir construtor;
* possuir getter e setter;
* sobrescrever pelo menos **1 método da superclasse**;
* utilizar `@Override`.

---

# 🌳 Organização das branches

O projeto deverá possuir:

```text
main
 │
 └── dev
      │
      ├── feature/superclasse
      │
      ├── feature/subclasse-a
      │
      └── feature/subclasse-b
```

### `main`

Representa a versão final e estável do projeto.

### `dev`

Será utilizada para integrar o trabalho da equipe.

### `feature/...`

Cada integrante deverá trabalhar em sua própria branch.

---

# ⚠️ Regra de ouro

> 🚫 Nenhum integrante poderá desenvolver diretamente na `main`.

Também não deverá ser realizado o desenvolvimento das funcionalidades diretamente na `dev`.

O código deverá chegar à `dev` por meio de **Pull Request**.

---

# 👤 PARTE 1 — Pessoa A

A Pessoa A será responsável por:

1. criar o repositório;
2. adicionar os colegas;
3. abrir o Codespace;
4. criar a branch `dev`;
5. criar a branch `feature/superclasse`;
6. criar a estrutura inicial do projeto;
7. implementar a superclasse;
8. criar commit;
9. realizar push;
10. abrir Pull Request.

---

# 🟢 Passo 1 — Criar o repositório

A Pessoa A deverá criar um novo repositório no GitHub.

Exemplo:

```text
sistema-animais-equipe
```

O repositório deverá possuir inicialmente:

```text
README.md
```

---

# 🟢 Passo 2 — Adicionar os colegas

No GitHub:

```text
Repositório
↓
Settings
↓
Collaborators
↓
Add people
```

Adicionar os outros dois integrantes.

As Pessoas B e C deverão aceitar o convite antes de continuar.

---

# 🟢 Passo 3 — Abrir o Codespace

No repositório:

```text
Code
↓
Codespaces
↓
Create codespace on main
```

O Codespaces será aberto diretamente no navegador.

---

# 🟢 Passo 4 — Abrir o terminal

No Codespaces:

```text
Terminal
↓
New Terminal
```

Execute:

```bash
git status
```

Depois:

```bash
git branch
```

A branch atual aparecerá marcada com `*`.

Exemplo:

```text
* main
```

---

# 🟢 Passo 5 — Criar a branch dev

Execute:

```bash
git switch -c dev
```

Confira:

```bash
git branch
```

Resultado esperado:

```text
* dev
  main
```

Envie a branch:

```bash
git push -u origin dev
```

---

# 🟢 Passo 6 — Criar a branch da superclasse

A Pessoa A não deverá programar diretamente na `dev`.

Execute:

```bash
git switch -c feature/superclasse
```

Confira:

```bash
git branch
```

Resultado esperado:

```text
  dev
* feature/superclasse
  main
```

---

# 🟢 Passo 7 — Criar a estrutura do projeto

Estrutura sugerida:

```text
src
└── main
    └── java
        └── projeto
           ├── Main.java
           └── modelo
               └── Superclasse.java
```

Posteriormente:

```text
modelo
├── Superclasse.java
├── SubclasseA.java
└── SubclasseB.java
```

Exemplo:

```text
modelo
├── Animal.java
├── Cachorro.java
└── Gato.java
```

---

# 🟢 Passo 8 — Desenvolver a superclasse

A Pessoa A deverá implementar a superclasse conforme os requisitos.

Também deverá criar inicialmente:

```text
Main.java
```

---

# 🟢 Passo 9 — Testar no Codespaces

Se o projeto utilizar Maven:

```bash
mvn clean compile
```

Resultado esperado:

```text
BUILD SUCCESS
```

Também poderá executar o `Main.java` pelo botão:

```text
Run
```

> ⚠️ Não faça commit de código que não compila.

---

# 🟢 Passo 10 — Alterar o README

O README deverá possuir a linha:

```text
Integrantes que contribuíram nesta versão: [PREENCHER]
```

A Pessoa A deverá alterar para:

```text
Integrantes que contribuíram nesta versão: [Pessoa A]
```

Essa linha será utilizada posteriormente para praticar conflitos.

---

# 🟢 Passo 11 — Verificar alterações

Execute:

```bash
git status
```

Depois:

```bash
git diff
```

Analise o que foi alterado.

---

# 🟢 Passo 12 — Criar o commit

Execute:

```bash
git add .
```

Depois:

```bash
git commit -m "feat: cria superclasse e estrutura inicial"
```

---

# 🟢 Passo 13 — Enviar a branch

Execute:

```bash
git push -u origin feature/superclasse
```

---

# 🟢 Passo 14 — Abrir Pull Request

No GitHub:

```text
base: dev
compare: feature/superclasse
```

Ou seja:

```text
feature/superclasse
        ↓
       dev
```

Clique em:

```text
Create pull request
```

---

# 👀 Passo 15 — Code Review

Outro integrante deverá revisar o Pull Request.

O revisor deverá verificar:

* se a classe está correta;
* se os atributos são privados;
* se existe construtor;
* se existem getters e setters;
* se o código está organizado;
* se o código compila.

Se estiver correto:

```text
Approve
```

Depois:

```text
Merge pull request
```

---

# 👤 PARTE 2 — Pessoa B

A Pessoa B será responsável pela primeira subclasse.

Exemplos:

```text
Cachorro
Guerreiro
Desenvolvedor
Carro
Livro
ContaCorrente
```

---

# 🔵 Passo 1 — Abrir o mesmo repositório

A Pessoa B deverá acessar o mesmo repositório criado pela Pessoa A.

> ⚠️ Não deverá criar outro repositório.

---

# 🔵 Passo 2 — Abrir o Codespace

No GitHub, selecione a branch:

```text
dev
```

Depois:

```text
Code
↓
Codespaces
↓
Create codespace on dev
```

Cada integrante utilizará seu próprio Codespace.

---

# 🔵 Passo 3 — Conferir a branch

Execute:

```bash
git branch
```

Resultado esperado:

```text
* dev
```

---

# 🔵 Passo 4 — Atualizar a dev

Execute:

```bash
git pull origin dev
```

---

# 🔵 Passo 5 — Criar a branch

Execute:

```bash
git switch -c feature/subclasse-a
```

Confira:

```bash
git branch
```

Resultado esperado:

```text
  dev
* feature/subclasse-a
```

---

# 🔵 Passo 6 — Desenvolver a subclasse

A Pessoa B deverá implementar a primeira subclasse.

Ela deverá possuir:

* `extends`;
* atributo próprio;
* construtor;
* getter;
* setter;
* `@Override`.

---

# 🔵 Passo 7 — Alterar o README

A Pessoa B deverá alterar:

```text
Integrantes que contribuíram nesta versão: [...]
```

para:

```text
Integrantes que contribuíram nesta versão: [Pessoa B]
```

> ⚠️ Não se preocupe se o nome da Pessoa A desaparecer temporariamente nesta branch.

Isso faz parte do exercício.

---

# 🔵 Passo 8 — Testar

Execute:

```bash
mvn clean compile
```

Corrija os erros antes de continuar.

---

# 🔵 Passo 9 — Criar o commit

Execute:

```bash
git status
```

Depois:

```bash
git add .
```

Depois:

```bash
git commit -m "feat: implementa primeira subclasse"
```

---

# 🔵 Passo 10 — Atualizar a branch com a dev

Execute:

```bash
git fetch origin
```

Depois:

```bash
git rebase origin/dev
```

---

# ⚔️ Passo 11 — Resolver o conflito

Provavelmente haverá conflito no:

```text
README.md
```

O arquivo poderá apresentar:

```text
<<<<<<< HEAD
Integrantes que contribuíram nesta versão: [Pessoa A]
=======
Integrantes que contribuíram nesta versão: [Pessoa B]
>>>>>>> ...
```

Isso significa que a mesma linha foi alterada por duas pessoas.

A linha correta deverá ficar:

```text
Integrantes que contribuíram nesta versão: [Pessoa A, Pessoa B]
```

Remova também:

```text
<<<<<<< HEAD
=======
>>>>>>>
```

Depois execute:

```bash
git add README.md
```

Em seguida:

```bash
git rebase --continue
```

---

# 🔵 Passo 12 — Conferir

Execute:

```bash
git status
```

Se não houver conflitos pendentes, continue.

---

# 🔵 Passo 13 — Enviar a branch

Execute:

```bash
git push -u origin feature/subclasse-a
```

Se a branch já tiver sido enviada antes do `rebase`, poderá ser necessário:

```bash
git push --force-with-lease
```

> ⚠️ Não utilize `git push --force`.

---

# 🔵 Passo 14 — Pull Request

No GitHub:

```text
base: dev
compare: feature/subclasse-a
```

Outro integrante deverá revisar.

Depois:

```text
Approve
```

e:

```text
Merge pull request
```

---

# 👤 PARTE 3 — Pessoa C

A Pessoa C deverá seguir o mesmo processo.

---

# 🟣 Passo 1

Abrir o mesmo repositório.

Selecionar:

```text
dev
```

Depois:

```text
Code
↓
Codespaces
↓
Create codespace on dev
```

---

# 🟣 Passo 2

Conferir:

```bash
git branch
```

---

# 🟣 Passo 3

Atualizar:

```bash
git pull origin dev
```

---

# 🟣 Passo 4

Criar:

```bash
git switch -c feature/subclasse-b
```

---

# 🟣 Passo 5

Implementar a segunda subclasse.

Exemplos:

```text
Gato
Mago
Gerente
Moto
Eletronico
ContaPoupanca
```

---

# 🟣 Passo 6

Alterar o README para:

```text
Integrantes que contribuíram nesta versão: [Pessoa C]
```

---

# 🟣 Passo 7

Testar:

```bash
mvn clean compile
```

---

# 🟣 Passo 8

Criar o commit:

```bash
git add .
```

```bash
git commit -m "feat: implementa segunda subclasse"
```

---

# 🟣 Passo 9

Atualizar:

```bash
git fetch origin
```

```bash
git rebase origin/dev
```

---

# 🟣 Passo 10

Resolver o conflito no README deixando:

```text
Integrantes que contribuíram nesta versão: [Pessoa A, Pessoa B, Pessoa C]
```

Depois:

```bash
git add README.md
```

```bash
git rebase --continue
```

---

# 🟣 Passo 11

Enviar:

```bash
git push -u origin feature/subclasse-b
```

---

# 🟣 Passo 12

Criar Pull Request:

```text
feature/subclasse-b
        ↓
       dev
```

Outro integrante deverá revisar.

---

# 🧪 PARTE 4 — Integração do Main.java

Depois que as duas subclasses estiverem integradas na `dev`, a equipe deverá testar o sistema completo.

Atualize a `dev`:

```bash
git switch dev
```

```bash
git pull origin dev
```

Crie uma nova branch:

```bash
git switch -c feature/integracao-main
```

---

# Implementar o Main

O programa deverá possuir:

* pelo menos 1 objeto da Subclasse A;
* pelo menos 1 objeto da Subclasse B;
* coleção utilizando o tipo da superclasse;
* chamadas de métodos sobrescritos;
* demonstração de polimorfismo.

Exemplo:

```java
List<Animal> animais = new ArrayList<>();

animais.add(new Cachorro(...));
animais.add(new Gato(...));

for (Animal animal : animais) {
    animal.executarAcao();
}
```

---

# Testar

Execute:

```bash
mvn clean compile
```

Depois execute o `Main.java`.

O programa deverá executar sem erros.

---

# Criar commit

```bash
git add .
```

```bash
git commit -m "feat: integra subclasses no programa principal"
```

Depois:

```bash
git push -u origin feature/integracao-main
```

Abra um Pull Request:

```text
feature/integracao-main
        ↓
       dev
```

Outro integrante deverá revisar.

---

# 🏁 PARTE 5 — Entrega final

Quando todo o sistema estiver funcionando na `dev`, deverá ser aberto o Pull Request final:

```text
dev
 ↓
main
```

No GitHub:

```text
base: main
compare: dev
```

---

# 🔎 Antes do merge final

Execute:

```bash
git switch dev
```

Depois:

```bash
git pull origin dev
```

Depois:

```bash
mvn clean compile
```

Resultado esperado:

```text
BUILD SUCCESS
```

Também execute o `Main.java`.

---

# 📝 README final

O README deverá conter:

```markdown
# Nome do Projeto

## Tema

Tema escolhido pela equipe.

## Integrantes

Nome: ____________________

RA: ______________________

Nome: ____________________

RA: ______________________

Nome: ____________________

RA: ______________________

## Hierarquia

Superclasse
├── Subclasse A
└── Subclasse B

## Descrição

Breve explicação sobre o funcionamento do sistema.

## Contribuições

Integrantes que contribuíram nesta versão: [Pessoa A, Pessoa B, Pessoa C]
```

---


# 📦 Entrega

A entrega deverá ser feita pelo Ulife e conter:

```text
Link do repositório GitHub
Nome completo dos integrantes
RA dos integrantes
```

O repositório deverá estar:

* público;

ou

* com acesso concedido à professora (rafapcmor), caso seja privado.

